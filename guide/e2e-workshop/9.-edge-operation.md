---
description: AWS IoT Greengrass를 활용한 GR00T VLA 모델 엣지 배포 MLOps 파이프라인
---

# 9. Edge Operation

이전 모듈에서 fine-tuning한 GR00T N1.6 모델을 [모듈 8](8.-evaluation.md)에서 시뮬레이션으로 검증했습니다. 이 모듈에서는 동일한 EC2 GPU 인스턴스에 **AWS IoT Greengrass**를 설치하여 **엣지 디바이스 환경을 가상으로 구축**하고, 학습된 모델을 Greengrass 컴포넌트로 배포하는 **MLOps 파이프라인**을 구성합니다.

### 이 모듈의 의의

실제 프로덕션에서는 학습된 모델을 Jetson AGX Thor 같은 엣지 디바이스에 배포해야 합니다. 하지만 워크숍 환경에서 물리 디바이스를 준비하기 어려우므로, **EC2 GPU 인스턴스를 엣지 디바이스로 간주**하여 동일한 배포 파이프라인을 체험합니다. 여기서 구축하는 Greengrass 기반 배포 구조는 실제 Jetson 디바이스에도 그대로 적용할 수 있습니다.

이를 통해 다음과 같은 **모델 학습 → 배포 → 운영**의 MLOps 전체 흐름을 완성합니다:

```
[모듈 6/7] 모델 학습 → [모듈 8] 시뮬레이션 검증 → [모듈 9] 엣지 배포 (본 모듈)
```

| 구분 | [모듈 8](8.-evaluation.md) (Closed-loop 평가) | 모듈 9 (Edge Operation) |
|------|----------------------------------------------|--------------------------|
| 목적 | 모델 품질 검증 (시뮬레이션) | 배포 파이프라인 구축 (MLOps) |
| 실행 환경 | Isaac Lab 컨테이너 (동일 인스턴스) | Greengrass Core (동일 인스턴스) |
| 모델 위치 | EFS / 로컬 | S3 → Greengrass 컴포넌트가 자동 다운로드 |
| 배포 방식 | docker run 수동 실행 | AWS IoT Greengrass 자동 배포 |
| 프로덕션 적용 | — | 동일 구조로 Jetson Thor fleet에 OTA 배포 가능 |

### 학습 목표

- AWS IoT Greengrass를 활용한 모델 배포 파이프라인 이해
- 컴포넌트 기반 배포 구조 (Docker 이미지 pull → 모델 준비 → TRT 최적화 → 추론 서버)
- TensorRT를 활용한 추론 가속 및 벤치마크
- 배포 상태 모니터링 (CloudWatch Logs, Greengrass CLI)


## 아키텍처 개요

```
┌─────────────────────────────────────────────────────────┐
│  AWS Cloud                                              │
│  ┌──────────┐  ┌──────────┐  ┌────────────────────┐   │
│  │ IoT Core │  │   ECR    │  │   S3 (모델/데이터)  │   │
│  └────┬─────┘  └────┬─────┘  └─────────┬──────────┘   │
│       │              │                   │              │
│  ┌────┴──────────────┴───────────────────┴──────────┐  │
│  │         Greengrass Deployment                     │  │
│  │  ┌─────────────┐ ┌───────────┐ ┌──────────────┐ │  │
│  │  │ setup       │ │ inference │ │ benchmark    │ │  │
│  │  │ (모델+TRT)  │ │ (서빙)    │ │ (성능측정)   │ │  │
│  │  └─────────────┘ └───────────┘ └──────────────┘ │  │
│  └──────────────────────────────────────────────────┘  │
│                         │                               │
└─────────────────────────┼───────────────────────────────┘
                          │ MQTT / TES
┌─────────────────────────┼───────────────────────────────┐
│  EC2 GPU Instance (L40S / Jetson Thor)                  │
│  ┌──────────────────────┴──────────────────────────┐   │
│  │  Greengrass Nucleus v2.17.0                      │   │
│  │  + Cli, LogManager, SecureTunneling              │   │
│  └──────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Docker (groot-n16-inference / groot-inference)   │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

## 인스턴스 접속

[모듈 1](../e2e-workshop/1.-isaaclab-infra-setup.md)에서 배포한 `IsaacLab-Stable-<userId>-Instance` EC2 인스턴스에서 작업합니다.

### code-server (VSCode) 접속

1. AWS 콘솔 → EC2 → 인스턴스에서 `IsaacLab-Stable-<userId>-Instance` 검색
2. Public IP 확인
3. 브라우저에서 `https://<Public-IP>:5555` 접속
4. Password: Secrets Manager에 저장된 값 (DCV 비밀번호와 동일)

시뮬레이션 실행 외에는 code-server 터미널에서 이후 모든 명령을 실행합니다.

---

## 사전 요구사항

### 1. EC2 인스턴스 Role에 IoT/Greengrass 권한 추가

Greengrass Nucleus 프로비저닝 시 IoT 리소스를 자동 생성하므로, EC2 인스턴스 Role에 아래 권한이 필요합니다.

```bash
# 인스턴스 Role 이름 확인
curl -s http://169.254.169.254/latest/meta-data/iam/security-credentials/

# 권한 추가
aws iam put-role-policy \
  --role-name <INSTANCE_ROLE_NAME> \
  --policy-name GreengrassProvisionPolicy \
  --policy-document '{
    "Version": "2012-10-17",
    "Statement": [
      { "Effect": "Allow", "Action": "iot:*", "Resource": "*" },
      { "Effect": "Allow", "Action": "greengrass:*", "Resource": "*" },
      {
        "Effect": "Allow",
        "Action": [
          "iam:GetRole", "iam:CreateRole", "iam:AttachRolePolicy",
          "iam:GetPolicy", "iam:PassRole", "iam:CreatePolicy", "iam:TagRole"
        ],
        "Resource": "*"
      },
      { "Effect": "Allow", "Action": "sts:GetCallerIdentity", "Resource": "*" }
    ]
  }'
```

> **참고**: 이 권한은 Greengrass 설치(프로비저닝) 시에만 사용됩니다. 설치 후 Greengrass 컴포넌트는 TES Role(GreengrassV2TokenExchangeRole)을 통해 AWS 서비스에 접근합니다.

### 2. 기타 요구사항

- Docker 설치 (nvidia-container-toolkit 포함)
- Java 11+ (Greengrass Nucleus 실행용)
- AWS CLI v2

## 설치

### N1.6 모델 배포

```bash
sudo bash setup-greengrass-workshop-N16.sh <USER_ID>
```


### 스크립트 실행 단계

| 단계 | 설명 |
|------|------|
| Step 1 | Prerequisites (JDK, unzip 등 설치) |
| Step 2 | S3 버킷 생성 + CloudFront에서 모델 다운로드 |
| Step 3 | ECR 리포지토리 생성 + Docker 이미지 빌드/Push |
| Step 3.5 | IAM 권한 확인 (경고만 출력) |
| Step 4 | Greengrass Nucleus 설치 + IoT 프로비저닝 + 시스템 컴포넌트 배포 |
| Step 5 | TES Role에 S3/ECR/CloudWatch 권한 추가 |
| Step 6 | Workshop 컴포넌트 등록 (S3에서 recipe 다운로드) |

### 설치 후 생성되는 리소스

| 리소스 | 이름 패턴 |
|--------|-----------|
| IoT Thing | `groot-{USER_ID}` |
| IoT Thing Group | `groot-{USER_ID}-group` |
| S3 Bucket | `groot-workshop-{USER_ID}-{ACCOUNT_ID}` |
| ECR Repository | `groot-n16-inference-{USER_ID}` (N1.6) / `groot-inference` (N1.7) |
| Greengrass Core Device | `groot-{USER_ID}` |

### 시스템 컴포넌트 (자동 배포)

| 컴포넌트 | 버전 | 설명 |
|----------|------|------|
| aws.greengrass.Nucleus | 2.17.0 | Greengrass 코어 |
| aws.greengrass.Cli | 2.17.0 | 디바이스 CLI |
| aws.greengrass.SecureTunneling | 2.0.0 | 원격 터널링 |
| aws.greengrass.LogManager | 2.3.12 | CloudWatch 로그 업로드 (workshop 컴포넌트 포함) |

## 컴포넌트 배포

### Workshop 컴포넌트

| 컴포넌트 | N1.6 버전 | N1.7 버전 | 설명 |
|----------|-----------|-----------|------|
| com.workshop.docker-build | 1.0.x | 2.0.x | ECR에서 Docker 이미지 Pull |
| com.workshop.setup | 1.0.x | 2.0.x | 모델 다운로드 + ONNX export + TRT 엔진 빌드 |
| com.workshop.inference | 1.0.x | 2.0.x | Policy Server 실행 (`gr00t.eval.run_gr00t_server`, ZMQ 포트 5555) |
| com.workshop.benchmark | 1.0.x | 2.0.x | PyTorch vs TRT 벤치마크 |

#### 컴포넌트 의존성 체인

```
com.workshop.docker-build (ECR pull)
    ↓ HARD dependency
com.workshop.setup (모델 + TRT 빌드)
    ↓ HARD dependency
com.workshop.inference (Policy Server)
    
com.workshop.benchmark (setup에 HARD 의존, inference와 독립)
```

#### Inference 컴포넌트 주의사항

- **서버 스크립트**: `python -m gr00t.eval.run_gr00t_server` (ZMQ REQ/REP 서버)
  - ❌ `standalone_inference_script.py`는 클라이언트/평가 스크립트 (서버 아님)
- **Lifecycle 유지**: `run` 스크립트가 종료되면 Greengrass가 shutdown을 호출하므로, 포트 확인 후 `docker wait`로 컨테이너 종료까지 대기해야 함
- **Greengrass 컴포넌트는 immutable**: 같은 버전으로 recipe를 덮어쓸 수 없음. 수정 시 버전 번호를 올려야 함

### 수동 배포 (설치 후)

#### 1단계: 환경 준비 (Docker 이미지 + 모델 + TRT 엔진)

ECR에서 Docker 이미지를 pull하고, S3에서 모델을 다운로드한 후 TRT 엔진을 빌드합니다. 최초 실행 시 약 10~15분 소요됩니다 (이후 재배포 시 캐시로 스킵).

```bash
aws greengrassv2 create-deployment \
  --target-arn arn:aws:iot:{REGION}:{ACCOUNT_ID}:thinggroup/groot-{USER_ID}-group \
  --deployment-name "workshop-n16-setup" \
  --components '{"aws.greengrass.Nucleus":{"componentVersion":"2.17.0"},"aws.greengrass.Cli":{"componentVersion":"2.17.0"},"aws.greengrass.SecureTunneling":{"componentVersion":"2.0.0"},"aws.greengrass.LogManager":{"componentVersion":"2.3.12"},"com.workshop.{USER_ID}.docker-build":{"componentVersion":"1.0.0"},"com.workshop.{USER_ID}.setup":{"componentVersion":"1.0.0"}}' \
  --deployment-policies '{"componentUpdatePolicy":{"action":"SKIP_NOTIFY_COMPONENTS"}}' \
  --region {REGION}
```

**결과 확인:**

```bash
# 배포 상태
aws greengrassv2 list-effective-deployments \
  --core-device-thing-name groot-{USER_ID} --region {REGION}
# → coreDeviceExecutionStatus: "SUCCEEDED"

# 컴포넌트 상태
aws greengrassv2 list-installed-components \
  --core-device-thing-name groot-{USER_ID} --region {REGION}
# → com.workshop.{USER_ID}.setup: lifecycleState "FINISHED"

# TRT 엔진 생성 확인 (인스턴스에서)
ls -lh /opt/groot/trt_n16/dit_model_bf16.trt
# → 약 2.1GB 파일 확인
```

#### 2단계: 벤치마크 실행

PyTorch vs TRT(DiT Action Head) 성능을 비교합니다. **inference와 동시 실행 시 GPU OOM이 발생하므로**, inference 없이 배포합니다.

```bash
aws greengrassv2 create-deployment \
  --target-arn arn:aws:iot:{REGION}:{ACCOUNT_ID}:thinggroup/groot-{USER_ID}-group \
  --deployment-name "workshop-n16-benchmark" \
  --components '{"aws.greengrass.Nucleus":{"componentVersion":"2.17.0"},"aws.greengrass.Cli":{"componentVersion":"2.17.0"},"aws.greengrass.SecureTunneling":{"componentVersion":"2.0.0"},"aws.greengrass.LogManager":{"componentVersion":"2.3.12"},"com.workshop.{USER_ID}.docker-build":{"componentVersion":"1.0.0"},"com.workshop.{USER_ID}.setup":{"componentVersion":"1.0.0"},"com.workshop.{USER_ID}.benchmark":{"componentVersion":"1.0.0"}}' \
  --deployment-policies '{"componentUpdatePolicy":{"action":"SKIP_NOTIFY_COMPONENTS"}}' \
  --region {REGION}
```

**결과 확인:**

```bash
# 벤치마크 결과 확인 (인스턴스에서)
grep -A5 "BENCHMARK RESULTS" /greengrass/v2/logs/com.workshop.${USER_ID}.benchmark.log
```

**기대 결과 (L40S):**

```
pytorch                   |   126.0ms avg |    7.9 Hz
trt_dit_action_head       |    60.1ms avg |   16.6 Hz
TRT Speedup: 2.10x faster than PyTorch
```

#### 3단계: 추론 서버 배포

Policy Server(ZMQ 포트 5555)를 시작합니다. Isaac Sim 시뮬레이션 평가에 사용됩니다. **벤치마크를 제외**하고 배포합니다.

```bash
aws greengrassv2 create-deployment \
  --target-arn arn:aws:iot:{REGION}:{ACCOUNT_ID}:thinggroup/groot-{USER_ID}-group \
  --deployment-name "workshop-n16-inference" \
  --components '{"aws.greengrass.Nucleus":{"componentVersion":"2.17.0"},"aws.greengrass.Cli":{"componentVersion":"2.17.0"},"aws.greengrass.SecureTunneling":{"componentVersion":"2.0.0"},"aws.greengrass.LogManager":{"componentVersion":"2.3.12"},"com.workshop.{USER_ID}.docker-build":{"componentVersion":"1.0.0"},"com.workshop.{USER_ID}.setup":{"componentVersion":"1.0.0"},"com.workshop.{USER_ID}.inference":{"componentVersion":"1.0.0"}}' \
  --deployment-policies '{"componentUpdatePolicy":{"action":"SKIP_NOTIFY_COMPONENTS"}}' \
  --region {REGION}
```

**결과 확인:**

```bash
# 컴포넌트 상태
aws greengrassv2 list-installed-components \
  --core-device-thing-name groot-{USER_ID} --region {REGION}
# → com.workshop.{USER_ID}.inference: lifecycleState "RUNNING"

# Policy Server 포트 확인 (인스턴스에서)
ss -tlnp | grep 5555
# → LISTEN 0.0.0.0:5555 확인
```

## 벤치마크 결과

### N1.6 (EC2 g6e.xlarge, NVIDIA L40S)

| 모드 | Avg Latency | P50 | P95 | P99 | Std | Frequency | Speedup |
|------|-------------|-----|-----|-----|-----|-----------|---------|
| PyTorch (BF16) | 126.0 ms | 125.5 ms | 128.4 ms | 137.5 ms | 2.5 ms | **7.9 Hz** | — |
| TRT (DiT Action Head) | 60.1 ms | 60.0 ms | 60.6 ms | 61.9 ms | 0.4 ms | **16.6 Hz** | **2.10x** |

> 50 iterations, 5 warmup. Greengrass `com.workshop.benchmark` v1.0.1 실행 결과.
> TRT는 DiT Action Head만 가속 (N1.6 방식). Backbone(Qwen3 VL)은 PyTorch 유지.

### N1.7 (EC2 g6e.4xlarge, NVIDIA L40S)

| 모드 | Avg Latency | P50 | P95 | Frequency | Speedup |
|------|-------------|-----|-----|-----------|---------|
| PyTorch (BF16) | 158.7 ms | 158.2 ms | 164.0 ms | **6.3 Hz** | — |
| TRT (Full Pipeline) | 50.8 ms | 50.8 ms | 52.2 ms | **19.7 Hz** | **3.13x** |

> N1.7은 Full Pipeline TRT (backbone + DiT 모두 가속).


## Isaac Sim Closed-loop 시뮬레이션 평가

Greengrass로 배포된 Policy Server(포트 5555)를 Isaac Sim과 연동하여 closed-loop 평가를 수행합니다.

### 사전 조건

- Policy Server가 포트 5555에서 실행 중 (Greengrass `com.workshop.inference` 또는 수동 docker run)
- DCV 데스크탑 세션 접속 (IsaacSim GUI 렌더링 필요)
- GPU 메모리 최소 20GB (Policy Server ~8GB + IsaacSim ~10GB)

```bash
# Policy Server 동작 확인
ss -tlnp | grep 5555
```

### Step 1: IsaacLab 환경 구성 + 컨테이너 진입

DCV 데스크탑 터미널에서 실행합니다. 최초 실행 시 LeIsaac 패키지, 씬 에셋, 평가 스크립트를 자동 설치합니다 (약 5~8분).

```bash
cd ~/aws-physical-ai-recipes/e2e-workshop/groot/inference
./run-isaaclab.sh
```

스크립트 완료 후 IsaacLab 컨테이너 쉘에 진입합니다.

### Step 2: Closed-loop 평가 실행

IsaacLab 컨테이너 내부에서 실행합니다:

```bash
/workspace/isaaclab/_isaac_sim/python.sh /workspace/scripts/evaluation/policy_inference.py \
    --task=LeIsaac-SO101-PickOrange-v0 \
    --eval_rounds=10 \
    --policy_type=gr00tn1.6 \
    --policy_host=localhost \
    --policy_port=5555 \
    --policy_action_horizon=16 \
    --policy_language_instruction="Pick up the orange and place it on the plate" \
    --device=cuda \
    --enable_cameras
```

**주요 파라미터:**

| 파라미터 | 설명 |
|----------|------|
| `--task` | LeIsaac 태스크 ID (SO-101 + 오렌지 집기 환경) |
| `--eval_rounds` | 평가 반복 횟수 (빠른 테스트: 3, 정식 평가: 10+) |
| `--policy_type` | `gr00tn1.6` (N1.7도 역호환으로 동일하게 사용) |
| `--policy_host` | Policy Server 주소 (`--network=host`이므로 localhost) |
| `--policy_port` | Policy Server ZMQ 포트 |
| `--policy_action_horizon` | GR00T이 한 번에 예측하는 action step 수 |
| `--policy_language_instruction` | 태스크 지시문 (자연어) |
| `--enable_cameras` | 카메라 렌더링 활성화 (observation 생성에 필수) |

### 정상 출력 예시

IsaacSim 초기화에 약 3~5분 소요된 후 평가가 시작됩니다:

```
[INFO] Loading task: LeIsaac-SO101-PickOrange-v0
[INFO] Policy client connected to localhost:5555
[INFO] Starting evaluation round 1/10...
[INFO] Round 1: SUCCESS (picked orange in 245 steps)
[INFO] Round 2: SUCCESS (picked orange in 312 steps)
...
[INFO] Evaluation complete: 7/10 success (70%)
```


## 모니터링

### 디바이스 배포 상태 확인

```bash
aws greengrassv2 list-effective-deployments \
  --core-device-thing-name groot-{USER_ID} \
  --region {REGION}
```

### 컴포넌트 로그 확인 (인스턴스에서)

```bash
# Greengrass 전체 로그
tail -f /greengrass/v2/logs/greengrass.log

# 개별 컴포넌트 로그
tail -f /greengrass/v2/logs/com.workshop.benchmark.log
tail -f /greengrass/v2/logs/com.workshop.inference.log
tail -f /greengrass/v2/logs/com.workshop.setup.log
```

### CloudWatch Logs (LogManager 연동)

LogManager가 아래 컴포넌트의 로그를 CloudWatch Logs로 자동 업로드합니다:
- `com.workshop.setup`
- `com.workshop.docker-build`
- `com.workshop.inference`
- `com.workshop.benchmark`

로그 그룹: `/aws/greengrass/GreengrassSystemComponent/{REGION}/groot-{USER_ID}`

### Greengrass CLI (인스턴스에서)

```bash
# 설치된 컴포넌트 목록
/greengrass/v2/bin/greengrass-cli component list

# 컴포넌트 상세 정보
/greengrass/v2/bin/greengrass-cli component details -n com.workshop.benchmark

# 로컬 배포 상태
/greengrass/v2/bin/greengrass-cli deployment status -l
```

## 제거 (Uninstall)

```bash
# N1.6 전체 제거
sudo bash setup-greengrass-workshop-N16.sh <USER_ID> --uninstall
```

### Uninstall 수행 내용

| 단계 | 작업 |
|------|------|
| 1/6 | Greengrass 서비스 중지 + `/greengrass/v2` 삭제 |
| 2/6 | Greengrass 배포 취소/삭제 + Core Device 삭제 |
| 3/6 | IoT Certificate 분리/삭제, Thing 삭제, Thing Group 삭제 |
| 4/6 | Greengrass 컴포넌트 삭제 (N1.6: 1.x 버전만, N1.7: 2.x 버전만) |
| 5/6 | TES Role 인라인 정책 제거 |
| 6/6 | ECR/S3 삭제 명령어 안내 (수동) |

> **참고**: ECR 리포지토리와 S3 버킷은 데이터 보호를 위해 자동 삭제하지 않습니다. 안내된 명령어로 수동 삭제하세요.

### Docker 이미지 구성 (N1.6 — 검증 완료)

```
Base: nvcr.io/nvidia/pytorch:24.12-py3
  - CUDA 12.4, Python 3.12, system TRT 10.7
  - torch 2.7.1+cu128 (uv sync)
  - transformers (latest, Qwen3 지원)
  - onnxscript, onnx, onnxruntime (venv에 설치 필수 — ONNX export용)
  - gr00t N1.6 (Isaac-GR00T commit 5dc80c4)
  
⚠️ 핵심: pip TRT 10.16.0.72 제거 → system TRT 10.7 심볼릭 링크
   (pip wheel TRT 10.16.0.72에 CUDA 초기화 버그 존재)

⚠️ onnxscript 설치 시 반드시 venv의 python을 지정:
   uv pip install --python /workspace/gr00t-repo/.venv/bin/python onnxscript onnx onnxruntime
   (system python에 설치하면 venv 내 torch가 찾지 못함)
```


## 트러블슈팅

### Greengrass 설치 실패: `iot:GetPolicy` 권한 없음

EC2 인스턴스 Role에 IoT 권한이 없습니다. [사전 요구사항](#1-ec2-인스턴스-role에-iotgreengrass-권한-추가) 참조.

### 배포 실패: `COMPONENT_VERSION_REQUIREMENTS_NOT_MET`

컴포넌트 간 의존성 버전 충돌입니다. 모든 컴포넌트를 동일 메이저 버전으로 배포하세요.

### 벤치마크 실패: `Unrecognized arguments`

Docker 이미지의 ENTRYPOINT와 충돌합니다. `--entrypoint /bin/bash`를 추가하고 `-c "cd /workspace/gr00t-repo && python ..."` 형태로 실행하세요.

### Core Device가 콘솔에 남아있음

`--uninstall` 시 Core Device도 삭제됩니다. 수동 삭제:
```bash
aws greengrassv2 delete-core-device --core-device-thing-name groot-{USER_ID} --region {REGION}
```

### TRT 벤치마크 실패: `CUDA initialization failure with error: 35`

pip으로 설치되는 TRT 10.16.0.72 wheel에 CUDA 초기화 버그가 있습니다. 해결:
- Docker 이미지 base를 `nvcr.io/nvidia/pytorch:24.12-py3`로 사용 (system TRT 10.7 포함)
- venv에서 pip TRT를 제거하고 system TRT 10.7을 심볼릭 링크

```bash
# 컨테이너 내부에서 실행
VENV_SP=/workspace/gr00t-repo/.venv/lib/python3.12/site-packages
SYS_SP=/usr/local/lib/python3.12/dist-packages
rm -rf $VENV_SP/tensorrt $VENV_SP/tensorrt_libs $VENV_SP/tensorrt_bindings $VENV_SP/tensorrt_cu12* $VENV_SP/tensorrt-* $VENV_SP/tensorrt_*.dist-info
ln -sf $SYS_SP/tensorrt $VENV_SP/tensorrt
[ -d $SYS_SP/tensorrt_libs ] && ln -sf $SYS_SP/tensorrt_libs $VENV_SP/tensorrt_libs
[ -d $SYS_SP/tensorrt_bindings ] && ln -sf $SYS_SP/tensorrt_bindings $VENV_SP/tensorrt_bindings
```

### TRT 엔진 빌드 실패: OOM (exit code 137)

ONNX export 시 메모리 부족. `com.workshop.setup` 컴포넌트가 재시작되면 자동 재시도됩니다. 다른 GPU 사용 컨테이너를 중지한 후 재배포하세요.

---
